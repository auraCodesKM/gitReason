# Leave empty for local dev — Vite's dev server proxies /api/* to the
# backend (see vite.config.js), so relative paths just work.
#
# In production (Vercel), set this to the deployed Render backend's URL,
# no trailing slash:
#   VITE_API_URL=https://gitreason-api.onrender.com
VITE_API_URL=
